[container.image]
repository= "anuruddhal" # optional default is local
name="hello-api" # optional
tag="sample2"  # default is latest

[cloud.deployment]
internal_domain_name="hello_svc"

[settings]
buildImage = false
